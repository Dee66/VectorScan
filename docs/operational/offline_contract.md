# Offline-by-default contract (placeholder)

VectorScan must operate without network access unless opt-in flags are set.
# TODO: document env vars such as VSCAN_ALLOW_NETWORK, VSCAN_ALLOW_LEAD_POST, and default behaviors for CLI + lead capture.
