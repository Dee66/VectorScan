[build-system]
requires = ["setuptools>=67", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "guardsuite-core"
version = "2.3.0"
description = "GuardSuite shared core shim to satisfy VectorScan dependency pins."
authors = [{ name = "GuardSuite", email = "engineering@guardsuite.dev" }]
license = { text = "Proprietary" }
requires-python = ">=3.9"
dependencies = []

[project.urls]
homepage = "https://guardsuite.example/internal"
